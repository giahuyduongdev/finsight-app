# Specs

Create one folder per feature or refactor:

```text
docs/specs/<name>/
  requirements.md
  design.md
  tasks.md
```

Use the templates in `../process/templates/` and the workflows in `../process/workflows/`.
