Starting CodeRabbit review in plain text mode...

Review directory: /mnt/c/Users/Admin/Desktop/finsight

Connecting to review service

============================================================================
File: client/src/hooks/use-auth-expiration.ts
Line: 61 to 88
Type: nitpick

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @client/src/hooks/use-auth-expiration.ts around lines 61 - 88, The effect may let a pending handleTokenRefresh complete after expiresAt changes and overwrite newer credentials; add a ref-based request id or AbortController to ignore/stale-cancel in-flight refreshes: create a ref like refreshRequestIdRef, increment it before calling refreshToken in handleTokenRefresh, capture the id in the async closure and only dispatch updateCredentials or logout if the captured id equals refreshRequestIdRef.current (or use an AbortController and pass its signal to refreshToken if supported), and on effect cleanup increment/abort to mark the in-flight request stale; update the handleTokenRefresh and the effect cleanup accordingly (references: handleTokenRefresh, refreshToken, updateCredentials, logout, expiresAt).

============================================================================
File: client/src/pages/settings/_components/account-form.tsx
Line: 164 to 170
Type: potential_issue

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @client/src/pages/settings/_components/account-form.tsx around lines 164 - 170, The file input lacks an associated label; update the account form so the FormLabel for the profile picture uses an htmlFor that matches an id on the Input used for avatar upload (the Input component and handleAvatarChange are in _components/account-form.tsx). Prefer generating a stable id (e.g., via React's useId() or a unique string) and set Input id={avatarId} and FormLabel htmlFor={avatarId} to ensure proper label association for screen readers; also ensure the Input retains accept="image/*" and onChange={handleAvatarChange}.

============================================================================
File: backend/src/config/google-ai.config.ts
Line: 116 to 123
Type: nitpick

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @backend/src/config/google-ai.config.ts around lines 116 - 123, The current retry logic uses a fixed 2s delay for 503/504/DEADLINE; update this to implement exponential backoff for 429 (rate limit) responses: inside the same retry loop that checks msg (the block containing msg.includes('503'|'504'|'DEADLINE')), add handling for msg.includes('429') that uses an increasing backoffDelay (e.g., backoffDelay = Math.min(baseDelay  2*attempt, maxDelay)) plus small jitter, incrementing attempt/retryCount each retry and capping retries; keep the existing fixed delay for 503/504/DEADLINE or optionally treat them with a separate backoff policy, and ensure you reset the backoff counter on a successful request. Reference the existing symbols msg, delay(), and the retry loop/attempt counter to locate where to add backoff logic.

============================================================================
File: client/src/components/navbar/logout-dialog.tsx
Line: 62 to 70
Type: nitpick

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @client/src/components/navbar/logout-dialog.tsx around lines 62 - 70, The Confirm Logout footer should avoid Tailwind's important modifier and add a Cancel button: replace the "!bg-red-500" usage on the primary Button with a normal Tailwind class (e.g., "bg-red-500 hover:bg-red-600" or adjust class order) so styles are not forced with "!"; keep existing isPending/Loader and handleLogout for the primary action. Add a secondary "Cancel" Button that calls the dialog close handler (use the component's existing onClose/closeDialog/setOpen(false) handler) and is keyboard-focusable, ensuring it is rendered alongside the primary Button to improve UX and accessibility.

============================================================================
File: client/src/components/transaction/transaction-form.tsx
Line: 144 to 145
Type: nitpick

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @client/src/components/transaction/transaction-form.tsx around lines 144 - 145, Replace the double type assertion on the zod resolver by declaring explicit input/output types for the form: derive FormInput and FormOutput from the existing formSchema using z.input and z.output, then call useForm with the generics  and pass zodResolver(formSchema) directly (remove "as unknown as Resolver" and stop asserting to Resolver). This change targets the useForm call and the formSchema symbol so the zodResolver types match without bypassing type safety.

============================================================================
File: backend/src/workers/report.worker.ts
Line: 136 to 147
Type: potential_issue

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @backend/src/workers/report.worker.ts around lines 136 - 147, The retry condition allows one extra execution because job.attemptsMade is incremented after a failure; update the if-check in the catch block to use the started counter: compute maxAttempts = job.opts?.attempts ?? 1 and replace "if (job.attemptsMade < maxAttempts)" with "if (job.attemptsStarted < maxAttempts)" so the worker re-throws only when fewer than the allowed attempts have been started (references: job.attemptsMade, job.attemptsStarted, job.opts?.attempts, maxAttempts, throw error).

============================================================================
File: backend/src/workers/receipt.worker.ts
Line: 256 to 264
Type: potential_issue

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @backend/src/workers/receipt.worker.ts around lines 256 - 264, The final-attempt check is off-by-one: change the condition that decides when to emit the failure event so it only fires after all attempts are exhausted; replace the current comparison using job.attemptsMade and maxAttempts (currently job.attemptsMade >= maxAttempts - 1) with a check that ensures attemptsMade has reached the configured max (use job.attemptsMade >= maxAttempts) before calling getIO().to(userId).emit('receipt:scan-failed', { jobId: job.id, error: friendlyMessage }).

============================================================================
File: backend/package.json
Line: 68 to 69
Type: nitpick

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @backend/package.json around lines 68 - 69, Remove the deprecated type packages from package.json by deleting the "@types/helmet" and "@types/mongoose" dependency entries shown in the diff; then run your package manager (npm/yarn/pnpm) to update node_modules and the lockfile so the removed packages are reflected. Ensure no other code imports or relies on those @types packages; leave the bundled types from "helmet" and "mongoose" (e.g., mongoose@^8.23.0) as-is.

============================================================================
File: backend/package.json
Line: 76
Type: potential_issue

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @backend/package.json at line 76, Remove the deprecated dependency "@types/socket.io" from package.json to avoid type conflicts with Socket.IO v4 (which ships its own types); edit package.json's dependencies/devDependencies and delete the entry for "@types/socket.io" (the caret version ^3.0.1) and then run your package manager (npm/yarn/pnpm) to reinstall/update lockfile so only the built-in Socket.IO types (from the installed "^4.8.3" socket.io) remain.
Setting up
Summarizing

============================================================================
File: backend/src/utils/cache.util.ts
Line: 36 to 39
Type: nitpick

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @backend/src/utils/cache.util.ts around lines 36 - 39, Replace the variadic spread call to Redis UNLINK with the array form to be cleaner and avoid unnecessary use of the spread operator: in the block where keys is built and used (the redis.unlink(...keys) call that updates totalDeleted), call redis.unlink(keys) instead of redis.unlink(...keys) and keep the await and totalDeleted += keys.length logic unchanged.

Review completed: 10 findings ✔
