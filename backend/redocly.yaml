extends:
  - recommended

rules:
  # The committed contract is intentionally local-first for development and Postman import.
  no-server-example.com: off
  # OAuth endpoints are redirect-only flows and correctly document 302 responses.
  operation-2xx-response: off
  operation-4xx-response: off
